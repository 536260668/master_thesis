# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## ⚠️ 重要注意事项（每次工作前必读）

1. **格式已设置，只生成内容**：论文LaTeX格式已由用户设置完成，撰写章节时只需生成正文内容，严格按照 `body/chap01.tex` 等现有文件的格式规范。

2. **Git提交规范**：
   - 提交者：`536260668 <536260668@qq.com>`
   - **严禁**在提交信息中包含任何Claude相关内容
   - **禁止**出现如"Generated with Claude Code"、"Co-Authored-By Claude"等字样

3. **撰写格式规范**：
   - 文件头：`%%% mode: latex` / `%%% TeX-master: t` / `%%% End:`
   - 章节：`\chapter{}`, `\section{}`, `\subsection{}`
   - 图表：图题在下、表题在上，使用三线表
   - 引用：`\cite{}`, `\ref{}`, `图~\ref{Figx-x}`
   - 标点：使用中文标点符号

---

## 项目概述

华中科技大学硕士学位论文写作项目。论文主题为**医疗大语言模型安全性评估**，基于英文论文《MLSE: Medical LLM Safety Evaluation in an Agentic Environment》进行改写和扩展。

### 参考资料位置

- `text_ref/MLSE Medical LLM Safety Evaluation in an Agentic Environment_final.docx` - 英文论文正文
- `text_ref/MLSE Medical LLM Safety Evaluation in an Agentic Environment Supplementary .docx` - 补充材料
- `text_ref/Figures.pdf` - 论文图表（框架图、实验结果图等）
- `text_ref/论文大纲.md` - **★ 核心参考：修订后的论文大纲（含创新点标注）**
- `格式参考.pdf` - HUST 论文格式规范文档

### 论文主要内容

- **研究问题**：医疗大语言模型（Medical LLM）在智能体环境中的安全性评估
- **核心贡献**：提出 MLSE 评估框架，包含智能体驱动的测试用例生成和多维度评估指标
- **本文创新点**：
  1. 错误放大因子 α 的数学定义
  2. ASR 作为安全下界代理指标
  3. PPL 作为攻击隐蔽性指标
  4. 语义漂移现象的形式化描述
  5. 供应链安全视角

---

## ⚠️⚠️⚠️ 论文分节生成工作流程 ⚠️⚠️⚠️

**【最高优先级】本项目采用"两阶段分节生成"策略**

### 核心原则

1. **减少无效上下文**：每次生成只聚焦于单个小节
2. **MD草稿优先**：先生成Markdown草稿文件，便于审阅和修改
3. **大纲驱动**：严格按照 `text_ref/论文大纲.md` 的结构生成
4. **原文对照**：结合英文原文 `text_ref/temp_main.md` 和 `text_ref/temp_supp.md`
5. **逻辑连贯**：确保行文逻辑通顺，符合学术写作规范

---

### 两阶段工作流程

#### 【阶段1】生成MD草稿文件

**目录结构：**
```
body/
├── drafts/                    # 草稿目录
│   ├── chap01/               # 第一章各小节
│   │   ├── 1.1.1_医疗AI范式转变.md
│   │   ├── 1.1.2_多智能体协作演进.md
│   │   ├── 1.1.3_安全威胁与挑战.md
│   │   └── ...
│   ├── chap02/               # 第二章各小节
│   │   ├── 2.1.1_Transformer架构.md
│   │   ├── 2.1.2_医疗指令微调.md
│   │   └── ...
│   ├── chap03/               # 第三章各小节
│   ├── chap04/               # 第四章各小节
│   ├── chap05/               # 第五章各小节
│   └── chap06/               # 第六章各小节
├── chap01.tex                # 最终LaTeX文件
├── chap02.tex
└── ...
```

**文件命名规范：**
```
{章节号}.{小节号}_{小节标题简写}.md
例如：
- 1.1.1_医疗AI范式转变.md
- 2.3.2_后门攻击原理.md
- 3.3.2_级联错误传播假设.md
```

**阶段1执行步骤：**
```
步骤1：确定目标小节
└── 用户指定章节号（如"1.1.1"）或小节标题

步骤2：读取相关资料
├── 阅读 text_ref/论文大纲.md 中对应小节的要点
├── 阅读 text_ref/temp_main.md 中的对应英文内容
└── 若涉及实验数据，阅读 text_ref/temp_supp.md

步骤3：生成MD草稿
├── 创建 body/drafts/chapXX/{小节号}_{标题}.md
├── 遵循学术写作规范
├── 确保与前一小节的逻辑衔接
└── 标注需要后续补充的内容（如 [待补充文献]）

步骤4：用户审阅
├── 用户检查内容、逻辑、措辞
├── 提出修改意见或直接编辑MD文件
└── 确认后继续下一小节
```

---

#### 【阶段2】汇总转换为LaTeX

**触发时机：** 当一章的所有小节MD草稿都审阅完毕后

**阶段2执行步骤：**
```
步骤1：汇总章节内容
├── 读取 body/drafts/chapXX/ 目录下所有MD文件
└── 按小节顺序合并内容

步骤2：转换为LaTeX格式
├── 添加文件头：%%% mode: latex 等
├── 转换章节标题：\chapter{}, \section{}, \subsection{}
├── 转换图表引用：图~\ref{}, 表~\ref{}
├── 转换文献引用：\cite{}
├── 转换公式：$$ ... $$
└── 保持中文标点符号

步骤3：写入最终文件
├── 写入 body/chapXX.tex
└── 更新 main.tex 的 \include{} 列表（如需）

步骤4：编译验证
└── 运行 xelatex 编译检查格式
```

---

### 分节生成命令示例

**阶段1命令：**
```
生成 1.1.1 医疗AI范式转变
生成 第二章 2.3节 对抗性攻击机制
生成 3.3.2 级联错误传播假设
```

**阶段2命令：**
```
汇总第二章为LaTeX
转换 chap03 为LaTeX格式
```

---

## ⚠️ 中国硕士毕业论文写作规范 ⚠️

**【最重要】以下规范必须严格遵守，生成的内容必须符合中国学术论文标准**

### 一、正文格式要求

1. **纯段落式叙述**：
   - 正文内部**禁止使用Markdown小标题**（如##、###）
   - 每个小节应由2-4个连贯的长段落组成
   - 段落之间用空行分隔，段内不换行

2. **段落结构**：
   - 每段字数建议200-400字
   - 每段应有明确的主题句（通常在段首）
   - 段落内部论述完整，逻辑自洽

3. **过渡与衔接**：
   - 段落间必须使用过渡词/句衔接
   - 常用过渡词：因此、综上所述、然而、值得注意的是、在此基础上、进一步地、与此同时、此外

### 二、语言风格要求

1. **正式学术语言**：
   - 使用"本文"而非"我们"或"我"
   - 使用书面语，避免口语化表达
   - 禁止使用：打破僵局、不堪设想、一针见血、不言而喻等口语/俗语

2. **客观陈述**：
   - 避免主观评价词（如"非常重要"、"极其优秀"）
   - 使用数据和事实支撑论点
   - 引用他人观点时明确标注来源

3. **术语规范**：
   - 专业术语首次出现需标注英文原文，格式：中文术语（English Term, 缩写）
   - 后续使用可直接用中文或缩写
   - 示例：大语言模型（Large Language Model, LLM）

### 三、禁止事项

❌ **禁止使用Markdown格式符号**：
- 不使用 `**加粗**`（正文中无需加粗）
- 不使用 `- 列表项`（改用"首先...其次...最后..."句式）
- 不使用 `## 小标题`（只在章节级别使用标题）
- 不使用表格（除非是正式的数据表格）

❌ **禁止醒目标注**：
- 不使用 `【★ 本文创新】` 等标注
- 创新点应自然融入论述，通过"本文首次提出..."、"区别于现有研究..."等表述体现

❌ **禁止口语化表达**：
- 不使用"打破了这一僵局"→改为"解决了这一问题"
- 不使用"后果不堪设想"→改为"可能造成严重后果"
- 不使用短句堆砌→改为长句复合结构

### 四、图表占位符规范

**【重要】论文中图表需人工手动生成，撰写时使用占位符标记**

#### 图片占位符格式

在正文中需要插入图片的位置，使用以下格式标注：

```
[图片占位符：第X章 图X-X 图片描述文字]

图片内容说明：
- 图表类型：框架图/流程图/柱状图/折线图/热力图等
- 数据来源：text_ref/Figures.pdf 第X页 / 实验数据 / 需手动绘制
- 核心要素：图中应包含的主要组件、标注、数据趋势等
- 备注：特殊绘制要求

[图片占位符结束]
```

**示例：**

```
[图片占位符：第三章 图3-2 MLSE评估框架整体架构]

图片内容说明：
- 图表类型：系统框架图
- 数据来源：text_ref/Figures.pdf 第3页
- 核心要素：
  * 测试用例生成模块（智能体驱动）
  * 多维度评估指标模块（ASR、PPL、COH等）
  * 目标医疗LLM
  * 评估结果输出
- 备注：需体现模块间的数据流向

[图片占位符结束]
```

#### 表格占位符格式

在正文中需要插入表格的位置，使用以下格式标注：

```
[表格占位符：第X章 表X-X 表格标题]

表格内容说明：
- 表格类型：实验结果对比/参数配置/指标定义等
- 数据来源：text_ref/temp_supp.md / text_ref/Figures.pdf / 实验数据
- 表头设计：列名及顺序
- 数据行数：约X行
- 备注：使用三线表格式

[表格占位符结束]
```

**示例：**

```
[表格占位符：第四章 表4-1 不同投毒比例下的模型性能对比]

表格内容说明：
- 表格类型：实验结果对比表
- 数据来源：第四章实验结果
- 表头设计：投毒比例 | 准确率 | ASR | PPL | COH
- 数据行数：约5行（0%, 1%, 5%, 10%, 20%）
- 备注：使用三线表，数值保留3位小数

[表格占位符结束]
```

#### 占位符使用原则

1. **精准定位**：占位符应紧跟在需要插入图表的文字描述之后
2. **详细说明**：清晰描述图表应包含的内容，便于后续手动生成
3. **数据指引**：明确标注数据来源，方便查找原始资料
4. **LaTeX转换**：阶段2转换为LaTeX时，占位符转换为注释格式：
   ```latex
   % [图片占位符：第三章 图3-2 MLSE评估框架整体架构]
   % \begin{figure}[htbp]
   %   \centering
   %   \includegraphics[width=0.8\textwidth]{figures/chap03/mlse_framework.png}
   %   \caption{MLSE评估框架整体架构}
   %   \label{Fig:3-2}
   % \end{figure}
   ```

### 五、文献引用规范

1. **引用位置标记**：
   - 使用 `[?]` 标记需要补充引用的位置
   - 格式：某研究表明[?]、根据文献[?]

2. **引用密度**：
   - 研究现状部分每段应有2-4处引用
   - 背景介绍部分每段应有1-2处引用

3. **引用方式**：
   - 观点引用："张三等[?]认为..."
   - 方法引用："采用文献[?]提出的方法"
   - 数据引用："实验结果表明[?]..."

### 六、各章节写作要点

| 章节 | 段落风格 | 要点 |
|------|---------|------|
| 绪论-背景 | 宏大叙事→聚焦到具体问题 | 从领域发展趋势切入 |
| 绪论-现状 | 文献综述式，按时间/类别组织 | 大量引用，客观陈述 |
| 绪论-意义 | 递进式论述 | 学术意义+现实意义 |
| 理论基础 | 教科书式定义+应用说明 | 为后文铺垫 |
| 方法设计 | 技术文档式 | 架构→模块→细节 |
| 实验分析 | 实验报告式 | 设置→结果→分析 |
| 总结展望 | 总结式 | 贡献回顾+局限反思 |

### 七、正确示例

**错误写法**（Markdown列表+口语化）：
```
## 数据层攻击

**数据投毒攻击。** 通过注入恶意样本来操纵模型行为，后果不堪设想。

- 攻击类型一：xxx
- 攻击类型二：xxx
```

**正确写法**（段落式+学术语言）：
```
数据层攻击主要包括数据投毒攻击和后门攻击两类。数据投毒攻击通过在模型训练数据中注入经过精心设计的恶意样本，使目标模型在特定输入条件下产生预设的错误输出。相关研究[?]表明，即使投毒比例低至1%，也可能导致模型在特定查询类型上产生系统性错误，在医疗场景中可能造成严重的临床后果。与数据投毒相比，后门攻击具有更强的隐蔽性。该类攻击在模型中植入特定的触发机制，使模型在正常情况下表现良好，仅在输入包含预设触发词时激活恶意行为。由于后门仅在特定条件下触发，传统的模型测试方法难以有效检测此类攻击[?]。
```

## 术语对照表（统一翻译）

| 英文术语 | 中文翻译 |
|---------|---------|
| Data Poisoning | 数据投毒 |
| Backdoor Attack | 后门攻击 |
| Prompt Injection | 提示词注入 |
| Jailbreaking | 越狱攻击 |
| Cascading Error | 级联错误 |
| Attack Success Rate (ASR) | 攻击成功率 |
| Perplexity (PPL) | 困惑度 |
| Coherence (COH) | 连贯性 |
| Agent | 智能体 |
| Multi-Agent System | 多智能体系统 |
| Standard Operating Procedure (SOP) | 标准作业程序 |
| Real-World Data (RWD) | 真实世界数据 |

---

## 编译文档

**使用 XeLaTeX 编译**（中文字体支持必需）：

```
xelatex main.tex
bibtex main
xelatex main.tex
xelatex main.tex
```

完整编译周期需要多次运行 XeLaTeX 以解析引用、文献目录和目录。

## 文档格式模式

通过修改 `main.tex` 中的文档类选项切换两种模式：

- **草稿格式**（工作草稿用）：`\documentclass[draftformat,mathCMR]{HUSTthesis}`
  - 包含页眉"华中科技大学硕士学位论文"
  - 包含页眉页脚装饰线

- **最终格式**（盲审/提交用）：`\documentclass[finalformat,mathCMR]{HUSTthesis}`
  - 移除页眉和装饰线
  - 页脚仅保留页码

在 `main.tex` 第 6-7 行注释/取消注释相应行来切换模式。

## 架构

### 核心文件

- `HUSTthesis.cls` - 主文档类，定义格式、版面、字体和环境
- `HUSTtils.sty` - 工具宏包，加载算法、表格等附加包
- `main.tex` - 根文档，组装所有章节

### 目录结构

- `body/` - 章节文件（chap01.tex ~ chap06.tex、cover.tex、conclusion.tex、ack.tex、appendix*.tex）
- `figures/` - 图片和图形（通过 `\graphicspath{{figures/}}` 引用）
- `font/` - 中文字体文件（xeCJK 使用的 .ttf 文件）
- `ref/` - 参考文献数据库（refs.bib）

### 封面信息（`body/cover.tex`）

所有论文元数据在 `body/cover.tex` 中定义：
- 中英文标题、作者、导师、学位信息
- 中英文摘要和关键词
- 分类号、学号、密级

编译前需更新此文件为个人信息。

### 章节组织

章节通过 `\include{body/chapXX}` 在 `main.tex` 中引入。结构如下：
- 前言部分（封面、摘要、目录）
- 正文部分（第1-6章）
- 后言部分（致谢、参考文献、附录）

### 字体配置

模板使用 `font/` 目录下的本地字体：
- `SimSun.ttf` - 中文正文字体（宋体）
- `SimHei.ttf` - 无衬线字体（黑体）
- `simkai.ttf` - 斜体/楷体
- `simfang.ttf` - 仿宋
- `STZhongsong.ttf` - 华文中宋

### 自定义字体命令

- `\song` 或 `\songti` - 宋体
- `\hei` 或 `\heiti` - 黑体
- `\kai` 或 `\kaishu` - 楷体
- `\fs` 或 `\fangsong` - 仿宋
- `\zhongsong` - 华文中宋

### 自定义字号命令

字号命令接受可选参数指定行距，如 `\sanhao[1.5]` 表示三号字1.5倍行距：
- `\dachu`, `\chuhao`, `\xiaochu`, `\yihao`, `\xiaoyi`, `\erhao`, `\xiaoer`, `\sanhao`, `\xiaosan`, `\sihao`, `\banxiaosi`, `\xiaosi`, `\dawu`, `\wuhao`, `\xiaowu` 等

## 常见操作

**添加新章节**：创建 `body/chap06.tex`，在 `main.tex` 中添加 `\include{body/chap06}`

**添加图片**：放入 `figures/` 目录，使用 `\includegraphics{filename}`

**添加引用**：使用 `\cite{key}` 或 `\citep{key}`（已配置 natbib 上标样式）

**添加文献**：编辑 `ref/refs.bib`，使用标准 BibTeX 格式

## 文档类选项

- `draftformat` / `finalformat` - 页眉页脚显示
- `mathtimes` / `mathCMR` - 数学字体（Times Roman 或 Computer Modern Roman）
- `arial` - 使用 Arial 字体作为无衬线字体

## 参考文献样式

使用 `HUSTThesis.bst` 样式，配合 natbib 宏包，文献引用格式为上标数字。

---

## 论文章节结构规划

根据修订后的大纲，论文章节安排如下：

| 章节 | 内容 | 预计字数 | 对应英文论文部分 |
|------|------|---------|-----------------|
| 第一章 绪论 | 研究背景、意义、现状、本文工作 | 5000字 | Introduction |
| 第二章 相关理论 | LLM、智能体、攻击机制、评估指标 | 4000字 | Background + Related Work |
| 第三章 框架设计 | MLSE框架、威胁建模、α定义 | 8000字 | Methodology (Framework) |
| 第四章 单体实验 | 数据投毒、提示词攻击、越狱攻击 | 6000字 | Single-Agent Experiments |
| 第五章 多智能体实验 | 级联失效、案例研究、可靠性验证 | 8000字 | Multi-Agent Experiments |
| 第六章 总结 | 总结、创新点、不足、展望 | 2000字 | Conclusion & Discussion |

### 章节完成情况追踪

| 章节 | 文件 | 状态 | 说明 |
|------|------|------|------|
| 第一章 绪论 | `body/chap01.tex` | ✅ 已完成 | 内容完整 |
| 第二章 相关理论 | `body/chap02.tex` | ⏳ 待撰写 | 按新大纲撰写 |
| 第三章 框架设计 | `body/chap03.tex` | ⏳ 待撰写 | 含错误放大因子α |
| 第四章 单体实验 | `body/chap04.tex` | ⏳ 待撰写 | 含补充实验 |
| 第五章 多智能体实验 | `body/chap05.tex` | ❌ 待创建 | 需新建文件 |
| 第六章 总结 | `body/chap06.tex` | ❌ 待创建 | 需新建文件 |
| 致谢 | `body/ack.tex` | ⏳ 待撰写 | |
| 附录 | `body/appendix*.tex` | ⏳ 待撰写 | |

---

## 写作注意事项

### 格式要求（依据格式参考.pdf）

1. **正文格式**：小四号（12pt）宋体，1.5倍行距，页边距符合模板设置
2. **章节标题**：
   - 章：黑体三号居中，单倍行距，段前段后各20磅
   - 节：黑体四号居左，1.5倍行距，段前24磅段后6磅
3. **图表格式**：
   - 图题在图下方，表题在表上方
   - 使用三线表（`\toprule`, `\midrule`, `\bottomrule`）
   - 图表按章编号（如图1.1、表2.1）
4. **公式格式**：按章编号（如式1.1），居中排版

### 翻译/改写原则

1. **学术规范**：翻译时保持学术语言风格，避免直译，注意中文学术表达习惯
2. **内容完整**：确保核心贡献、方法、实验结果完整呈现
3. **引用规范**：英文论文中的参考文献需要正确录入 `ref/refs.bib`
4. **图表处理**：从 `text_ref/Figures.pdf` 提取图片，放入 `figures/` 目录使用

---

## 下一步工作建议

按照分节生成策略，建议撰写顺序：

1. **第二章**：为后续章节奠定理论基础
   - 2.1 → 2.2 → 2.3 → 2.4 → 2.5
2. **第三章**：核心框架设计
   - 3.1 → 3.2 → 3.3（含α定义）→ 3.4 → 3.5
3. **第四章**：单体实验
4. **第五章**：多智能体实验
5. **第六章**：总结与展望
6. **附录**：补充材料

用户可随时指定从任意小节开始撰写。
